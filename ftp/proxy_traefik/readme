# mot de passe dashboard

apt install apache2-utils
htpasswd -nb admin ton_mot_de_passe
-> hash

mettre ici :
<fichier dynamic conf >
  middlewares:
    # Middleware for dashboard
    auth:
      basicAuth:
        users:
          - "xxx"

# creation fichier acme
cd <repertoire trefik>
rm -f acme.json
touch acme.json
chmod 600 acme.json


# creation reseau
docker network create proxy

